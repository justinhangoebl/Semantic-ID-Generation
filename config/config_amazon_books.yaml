# Data loader configuration
data:
  dataset: "amazon_books"
  batch_size: 16
  normalize_data: False
  embedding_dimension: "meta" # 'user' or 'item' or 'relation' or 'entity'

# Model configuration
model:
  input_dimension: 768
  hidden_dimensions: [1024, 512, 256]
  latent_dimension: 128
  num_codebook_layers: 3
  codebook_clusters: 256
  commitment_weight: 0.25
  # Quantization method: "ste" (Straight-Through Estimation) or "gumbel_softmax"
  quantization_method: "gumbel_softmax"

# Training configuration
train:
  learning_rate: 0.0003
  weight_decay: 0.0001
  num_epochs: 256

  # Temperature annealing (for Gumbel Softmax)
  temperature_annealing: True
  # Gumbel Softmax parameters
  temperature: 1.2
  min_temperature: 0.3
  temperature_decay: 0.995
  temperature_update_frequency: 1
  annealing_schedule: "exponential"

# Additional configuration
general:
  use_wandb: True
  wandb_project: "semantic_id_amazon_books"
  wandb_entity: "justin-hangoebl-master-thesis"
  save_model: True
